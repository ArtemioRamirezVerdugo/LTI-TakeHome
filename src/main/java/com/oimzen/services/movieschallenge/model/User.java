package com.oimzen.services.movieschallenge.model;

import lombok.Getter;
import lombok.Setter;
import lombok.ToString;

/**
 * Assuming this entity belongs to someone else
 * so only adding it as a placeholder to represent the relationship
 */
@Getter @Setter @ToString
public class User {

    private int id;
}
